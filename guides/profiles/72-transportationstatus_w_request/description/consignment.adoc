[[consignment]]
= Consignment

In this profile consignment is used to identify any transport events or transport handling units that it is requested status on.
If the Consignment ID is a GS1 GINC (Global Identification Number for Consignment), it should be indicated by the attribute @schemeID. If it is not, the attribute can be omitted.
If the Transport Handling Unit ID has the format of an SSCC, the attribute @schemeID should be included to indicate it.

Example:
[source, xml, indent=0]
----
include::{snippet-dir}/snippet-1.xml[tags=consignment]
----