== XML example

The following XML example illustrates the structure of an instance of {name-transaction}.

link:files/tender-instance-download.xml[Link to example file]


[source,xml,indent=0]
.Example used in document
----

include::../{xml-doc-file}[tags=**]

----
