[[parties]]
= Parties and roles

The following parties/roles may be specified in the message.
The same actor may play more than one role depending on the handling routine.

The diagram below shows the parties and roles in this profile.

image::images/roles.png[image,width=649,height=260]

[[sender-party]]
== Sender (SenderParty)
The Sender is the sender of the transaction. Normally the Transport User is the sender of the Transport Execution Plan Request and the Transport Service Provider  is the sender of the Transport Execution Plan. But the Transport Execution Plan may also be sent by the Transport User  in case of a change or cancellation.

Example:
[source, xml, indent=0]
----
include::{snippet-dir}/snippet-1.xml[tags=sender-party]
----

[[receiver-party]]
== Receiver (ReceiverParty)
The Receiver is the receiver of the transaction. Normally the Transport Service Provider is the receiver of the Transport Execution Plan Request and the Transport User is the receiver of the Transport Execution Plan. But the Transport Execution Plan may also be received by the Transport Service Provider in case of a change or cancellation.

Example:
[source, xml, indent=0]
----
include::{snippet-dir}/snippet-1.xml[tags=receiver-party]
----

[[transport-user-party]]
== Transport User (TransportUserParty)

The Transport User is the user of the transport service, often the Consignor. The transport User is the default sender of the Transport Execution Plan Request and the default receiver of the Transport Execution Plan.

Example:
[source, xml, indent=0]
----
include::{snippet-dir}/snippet-1.xml[tags=transport-user-party]
----

[[transport-service-provider-party]]
== Transport Service Provider (TransportServiceProviderParty)

The Transport Service Provider is the party providing the transport services. The Transport Service Provider is the default sender of the Transport Execution Plan and the default receiver of the Transport Executions Plan Request.

Example:
[source, xml, indent=0]
----
include::{snippet-dir}/snippet-1.xml[tags=transport-service-provider-party]
----

[[consignor-party]]
== Consignor (ConsignorParty)

Consignor, also known as the shipper, is the sender of the goods. The Consignor is often the buyer of the freight services. If the Consignor is the same for all consignments in the Waybill, the consignor can be described on the document level instead of in each consignment.

Example:
[source, xml, indent=0]
----
include::{snippet-dir}/snippet-1.xml[tags=consignor-party]
----

[[consignee-party]]
== Consignee (ConsigneeParty)

Consignee is known as the DeliveryCustomerParty in the Despatch and receive advice. The Consignee is the person or organisation to which the products will be shipped and who is taking possession. The role is carried out by the customer or on behalf of the customer. The Consignee is a mandatory party in the consignment element and can be the Transportation Buyer.

Example:
[source, xml, indent=0]
----
include::{snippet-dir}/snippet-1.xml[tags=consignee-party]
----

[[carrier-party]]
== Carrier (CarrierParty)

Carrier is the party providing the transport of goods between named points.

Example:
[source, xml, indent=0]
----
include::{snippet-dir}/snippet-1.xml[tags=carrier-party]
----

[[payee-party]]
== Payee (PayeeParty)

Used in the Transport Execution Plan Request to identify the party paying for the transport. NB! The Payee is normally seen as the party receiving the payment, and the name of this role will be changed in the next UBL-version.

Example:
[source, xml, indent=0]
----
include::{snippet-dir}/snippet-1.xml[tags=carrier-party]
----

[[billto-party]]
== Bill To (BillToParty)

Used in the Transport Execution Plan to identify the party paying for the transport.

Example:
[source, xml, indent=0]
----
include::{snippet-dir}/snippet-1.xml[tags=carrier-party]
----

[[notify-party]]
== Notify (NotifyParty)

The Notify party is an external party that needs to be notified regarding the transport of the Waybill. This can be an agent responsible for the insurance of the goods or a public authority if the consignment contains hazardous goods. For the Notifier and Contact party, the contact element is mandatory.

Example:
[source, xml, indent=0]
----
include::{snippet-dir}/snippet-1.xml[tags=notify-party]
----