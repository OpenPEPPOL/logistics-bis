[[package]]
= Package

A package describes a collection of goods items and is contained in a transport handling unit or another package. It describes how a transport handling unit is packed. The package is repeated for each physical packages there is a need for information about.

Example:
[source, xml, indent=0]
----
include::{snippet-dir}/snippet-1.xml[tags=package]
----
