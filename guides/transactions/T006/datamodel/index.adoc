== XML example

The following XML example illustrates the structure of an instance of {name-transaction}.

link:files/tender-receipt-instance.xml[Link to example file]

[source,xml,indent=0]
.Example
----

include::../{xml-doc-file}[tags=**]

----
